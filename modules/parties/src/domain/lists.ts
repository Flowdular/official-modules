/**
 * The id exports.core holds the party list under: the module id followed by
 * the list key. It lives here so the screen that starts a job and the server
 * that declares the list name one string without the client bundle importing
 * `@flowdular/sdk/server`.
 */
export const PARTY_EXPORT_LIST_ID = 'parties.core.records';
