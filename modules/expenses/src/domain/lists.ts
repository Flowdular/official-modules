/**
 * The id exports.core holds the claims list under: the module id followed by
 * the list key. It lives here so the screen and the server name one string
 * without the client bundle importing `@flowdular/sdk/server`.
 */
export const EXPENSES_CLAIMS_EXPORT_LIST_ID = 'expenses.core.claims';
