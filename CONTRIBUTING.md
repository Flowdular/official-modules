# Contributing

Change one module at a time. Preserve all applied migrations and deployed IDs.
Run `pnpm verify`, inspect the final diff, and update the version-bound review
record with requirements, findings and actual command outcomes. Release artifacts
are immutable. Changes to an approved specification require operator reapproval.
